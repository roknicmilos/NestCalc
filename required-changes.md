# TODO

1. Remove order numbers from the list "Faze otplate"
2. Text on the left side of the "Sačuvaj" button (e.g. "Sve izmene su sačuvane")
   should be moved under it and it should be bigger if action is required, it
   should be "more in your face" (if user needs to save the changes they
   applied)
3. Improve "Osnovni podaci" label, make it more descriptive/specific (e.g. "
   Osnovni podaci o kupovini")
4. Change color of the labels in "Dodatne pozajmice" cards. Don't use green
   color for "KEŠ KREDIT" and "POZAJMICA". Green should be used for non-debt
   stuff in the app.
5. Split "Pregled" section into sub-sections:
    - učešće-related section
    - leftover after učešće (for PPAP if applies, and "for other stuff")
    - stambeni-kredit-related stuff
6. Make total number of years and months in phases one of the primary info in
   each phase card, not something that should be in brackets using small font.

# TODO #2

1. Make sections on the left visually differentiated. For example, "Početni
   kapital" could use greenish bg color or something like that, and "Dodatne
   pozajmice" could use redish color.
